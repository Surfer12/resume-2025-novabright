import React from 'react';

const MetricsCard: React.FC<any> = (props) => {
  return (
    <div className="bg-white p-4 rounded-lg shadow">
      <p>Metrics Card Placeholder</p>
      {/* Later: Add props.metric.type, props.metric.value etc. */}
    </div>
  );
};

export default MetricsCard; 